{
  "description": "Показывает новые письма aixchange при старте сессии и перед сообщением пользователя. Когда нового нет, вывод пустой — контекст и токены не тратятся.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/axc\" inbox --brief 2>/dev/null || true",
            "timeout": 20
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/axc\" inbox --brief 2>/dev/null || true",
            "timeout": 20
          }
        ]
      }
    ]
  }
}
